# Music Library Client

## Start the client

```bash
npm install
npm start
```

The client serves at `http://localhost:5173` and expects the API to be running at `http://localhost:3000`.

## Run client functional tests

```bash
npm test
```
