# music-library

CET252 prototype submission is in `./cet252`.

Run both API and CLIENT together from repo root:

```bash
npm start
```

- API: `http://localhost:3001`
- API docs/root page: `http://localhost:3001/`
- Client: `http://localhost:4173`
